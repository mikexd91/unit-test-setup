pipeline {
	agent any

	stages {

		stage ("Setup NodeJS") {
			steps {
				nodejs(nodeJSInstallationName: 'Node v6.10.3 - install automatically') {
				    sh 'node --version';
				}
			}
		}
		
		stage ("install") {
			steps {
				echo "Install";
			}
		}

		stage ("Tests") {
			steps {
				parallel (

					eslint: {
						echo "eslint";
					},

					karma: {
						echo "karma";
					}

				)
			}
		}

		stage ("checkstyle") {
			steps {
				echo "Checkstyle";
			}
		}



	}
}